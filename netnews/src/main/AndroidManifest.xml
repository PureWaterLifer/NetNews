<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zzptc.sky.netnews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".NewsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/news_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.ui.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.ui.activities.NewsActivity" />
        <activity android:name=".mvp.ui.activities.AboutActivity" />
        <activity android:name=".mvp.ui.activities.NewsBrowserActivity" />
        <activity android:name=".mvp.ui.activities.NewsChannelActivity" />
        <activity android:name=".mvp.ui.activities.NewsDetailActivity" />
        <activity android:name=".mvp.ui.activities.NewsPhotoDetailActivity" />
        <activity android:name=".mvp.ui.activities.PhotoActivity" />
        <activity android:name=".mvp.ui.activities.PhotoDetailActivity"></activity>
    </application>

</manifest>